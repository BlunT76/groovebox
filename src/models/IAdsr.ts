export interface IAdsr
{
    /**
     * attack (time)
     */
    a: number;
    /**
     * decay (time)
     */
    d: number;
    /**
     * sustain (volume)
     */
    s: number;
    /**
     * release (time)
     */
    r: number;
}
